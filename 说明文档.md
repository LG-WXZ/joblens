# 职镜 JobLens 使用与部署说明

## 1. 项目定位

职镜 JobLens 是一个 AI 求职辅助与模拟面试智能体。用户上传招聘广告和简历后，系统会生成岗位真实性判断、岗位匹配分析、简历优化建议、知识点梳理、练习题和模拟面试反馈。

## 2. 本地运行

环境要求：

- Node.js 18 或更高版本
- npm 9 或更高版本

安装依赖：

```bash
npm install
```

复制环境变量模板：

```bash
copy .env.example .env.local
```

在 `.env.local` 中填写真实 API Key 后启动开发服务：

```bash
npm run dev
```

访问地址：

```text
http://127.0.0.1:3000
```

## 3. 主要环境变量

```text
DASHSCOPE_API_KEY      阿里云百炼 DashScope API Key
DASHSCOPE_API_BASE     OpenAI 兼容模式接口地址
TEXT_MODEL             文本生成模型
VISION_MODEL           图像/多模态解析模型
SILICONFLOW_API_KEY    旧版语音服务 Key，可选
```

注意：`.env.local` 包含密钥，已经在 `.gitignore` 中排除，不能提交到 GitHub。

## 4. 生产构建

```bash
npm run build
npm run start -- --hostname 127.0.0.1 --port 3001
```

构建成功后，Next.js 会生成可部署的生产版本。

## 5. 推荐长期部署方式

推荐使用 Vercel 部署，因为本项目是 Next.js 应用，并包含 API Routes。部署流程：

1. 将代码推送到 GitHub。
2. 登录 Vercel，选择 Import Git Repository。
3. 选择 GitHub 中的 JobLens 仓库。
4. 在 Vercel Project Settings 中添加 `.env.example` 所列环境变量。
5. 点击 Deploy，等待构建完成。

部署后 Vercel 会提供稳定的 `https://` 访问地址。语音录制、麦克风权限等浏览器能力在公网环境下建议使用 HTTPS。

## 6. GitHub 推送注意事项

- 不要提交 `.env.local`、日志文件、`.next`、`node_modules`。
- 首次推送前建议确认 `git status` 中没有密钥文件。
- GitHub 不支持账号密码直接推送代码，推荐使用 GitHub CLI 浏览器登录或 Personal Access Token。

## 7. 核心页面

- `/`：首页
- `/upload`：上传招聘信息和简历
- `/report`：综合分析报告
- `/practice`：定向练习题
- `/interview`：AI 模拟面试

## 8. 当前模型配置

- 文本生成：`qwen3.6-plus`
- 视觉解析：`qwen3.6-plus`
- 面试官语音合成：DashScope `qwen3-tts-instruct-flash`
- 面试官音色：`Maia`
